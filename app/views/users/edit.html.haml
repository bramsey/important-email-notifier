%h1 Edit User
%h2= link_to 'Manage Accounts', user_accounts_path( current_user )

= form_for(@user) do |f|
	= render 'fields', :f => f
	.actions
		= f.submit "Update"

%div
	= gravatar_for @user
	%a(href="http://gravatar.com/emails" target="_blank") change